# JTSN 배포 원칙

v5.61부터 모든 공개 업데이트는 같은 버전의 다음 파일을 함께 배포한다.

- `JTSN_Setup_vX.XX.exe`: 설치 및 자동 업데이트 실행 파일
- `JTSN_vX.XX_FULL_SOURCE.zip`: 해당 실행 파일을 다시 빌드할 수 있는 전체 원본 소스
- `JTSN_vX.XX_SHA256SUMS.txt`: 설치 파일과 소스 ZIP의 SHA-256 무결성 값
- Git 커밋과 `vX.XX` 태그

실행 파일만 단독으로 교체하거나 이전 실행 파일의 버전 문자열만 수정해 배포하지 않는다.
배포 전 본체와 설치 프로그램을 새로 빌드하고, 소스 ZIP을 별도 폴더에 풀어 재빌드할 수 있는지 확인한다.
